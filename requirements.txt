# Core
python-dotenv>=1.0.0
pandas>=2.0.0
PyYAML>=6.0
tabulate>=0.9.0

# UI
streamlit>=1.40.0

# Snowflake
snowflake-connector-python>=3.7.0

# Flat files — CSV / TSV / JSON work out of the box. Excel (.xlsx) needs openpyxl;
# Parquet needs pyarrow (pip install pyarrow) if you use it.
openpyxl>=3.1.0

# Tests
pytest>=8.0.0

# ---------------------------------------------------------------------
# MSSQL (optional — install only if you point a test case at SQL Server).
# pymssql is the easy path: one wheel, no system driver needed.
#   pip install pymssql
# pyodbc also works if you already have the Microsoft ODBC driver:
#   pip install pyodbc
# ---------------------------------------------------------------------
